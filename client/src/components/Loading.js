import React from 'react';
import ReactLoading from 'react-loading';
const Loading = ({ center }) => {

  

  return <div className='center'><ReactLoading type={'cylon'} color={'#2cb1bc'} height={'10%'} width={'10%'} /></div>
}

export default Loading
